# TODO: Deploy Backend & MongoDB on Render

- [x] 1. Create `render.yaml` at repository root
- [x] 2. Update `server/index.js` for production (trust proxy)
- [x] 3. Update `server/lib/passport.js` for dynamic OAuth callbacks
- [x] 4. Create `client/src/config.js` for centralized API base URL
- [x] 5. Update client files to use `config.js` instead of hardcoded localhost
- [x] 6. Update `server/.env.example` with all required Render env vars
- [x] 7. Create `DEPLOY_RENDER.md` with step-by-step deployment guide
- [x] 8. Final review and test

