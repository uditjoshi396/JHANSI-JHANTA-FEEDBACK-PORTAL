# TODO - Fix Vercel Deployment

- [x] Create root `vercel.json` in repo root to fix build commands
- [x] Update `client/vercel.json` to add SPA rewrite rules
- [x] Update `client/package.json` to add engines field
- [x] Create root `.vercelignore` to exclude server files

